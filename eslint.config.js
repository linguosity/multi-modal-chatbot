// Use Next.js default ESLint config
module.exports = {
  extends: [
    'next/core-web-vitals'
  ],
  rules: {
    // Add custom rules here if needed
  },
  ignorePatterns: [
    'node_modules',
    '.next',
    'dist',
    'build'
  ]
};